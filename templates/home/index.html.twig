{% extends 'base.html.twig' %}

{% block title %}Accueil — HighlightForge{% endblock %}

{% block body %}
<div class="relative w-screen left-1/2 -translate-x-1/2 h-[50vh] -mt-8">

    {# Image de fond #}
    <div class="absolute inset-0 bg-[url(/images/bgHome.png)] bg-cover bg-center"></div>

    {# Fondu vers le bas en transparent #}
    <div class="absolute inset-0" style="background: linear-gradient(to bottom, transparent 30%, rgb(17,24,39) 100%)"></div>

    {# Contenu par dessus #}
    <div class="relative z-10 flex flex-col items-center justify-center h-full text-center px-6">
        <h1 class="text-5xl font-bold text-indigo-400 mb-4">⚔️ HighlightForge</h1>
        <p class="text-gray-400 text-xl mb-8">
            La communauté des passionnés de figurines miniatures
        </p>
        {% if not app.user %}
            <div class="flex gap-4 justify-center">
                <a href="{{ path('app_register') }}" 
                   class="bg-indigo-600 hover:bg-indigo-700 px-8 py-3 rounded-lg text-lg transition">
                    Rejoindre la communauté
                </a>
                <a href="{{ path('app_login') }}" 
                   class="bg-gray-700 hover:bg-gray-600 px-8 py-3 rounded-lg text-lg transition">
                    Se connecter
                </a>
            </div>
        {% else %}
            <p class="text-green-400 text-lg">
                Bienvenue, {{ app.user.username }} ! 👋
            </p>
        {% endif %}
    </div>

</div>
{% endblock %}